node_modules/
dist/
.env
.env.*
!.env.example
/data/
logs/
*.log

# Claude Code local/session state — never shared, can contain full worktree copies
.claude/settings.local.json
.claude/worktrees/

# Marketing assets — not app source, keep them out of the deploy repo
*.mp4

# Personal reference docs — not app source
payment-provider-guide.html

# Raw asset uploads / drafts — the copy actually used by code lives under
# server/assets/ (see server/services/email.js); these are not app source
UPDATE LOGO.jpeg
LANDING PAGE/
